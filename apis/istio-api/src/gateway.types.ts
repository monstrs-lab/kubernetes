/* eslint-disable no-shadow */

export enum GatewayResourceVersion {
  v1alpha2 = 'v1alpha3',
}

export enum GatewayResourceKind {
  Gateway = 'Gateway',
}

export enum GatewayResourceGroup {
  Gateway = 'gateways',
}

export enum GatewayDomain {
  Group = 'networking.istio.io',
}
